# Execution Policy (human)
- Never delete files outside the project directory.
- Prompt before running: pip install, conda, docker, network calls, or solver downloads.
- Allow: python -m pytest, python -m src.solve, formatting tools.

